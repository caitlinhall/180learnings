'use strict';

describe('myApp.day6 module', function() {

  beforeEach(module('myApp.day6'));

  describe('day6 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('Day6Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});