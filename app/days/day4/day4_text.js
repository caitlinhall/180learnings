'use strict';

describe('myApp.day4 module', function() {

  beforeEach(module('myApp.day4'));

  describe('day4 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('Day4Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});