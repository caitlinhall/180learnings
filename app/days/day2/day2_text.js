'use strict';

describe('myApp.day2 module', function() {

  beforeEach(module('myApp.day2'));

  describe('day2 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('Day2Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});